defaults: &defaults
  adapter: postgresql
  encoding: utf-8
  pool: 5
  host: localhost
  username: superuser
  password: superpass

development:
  <<: *defaults
  database: myMusicDatabase_development

test:
  <<: *defaults
  database: myMusicDatabase_test

